pipeline {
    agent any
    tools {
        nodejs 'nodenode'
    }
    stages {
        stage('Install Packages') {
            steps {
                dir('Front') {
                    sh 'npm install'
                }
            }
        }
        stage('Build') {
            steps {
                dir('Front') {
                    sh 'npm run build'
                }
            }
        }
        stage('Test') {
            steps {
                dir('Front') {
                    sh 'npm run test --watch=false --code-coverage'
                }
            }
        }
        stage('Sonarqube Report') {
            steps {
                dir('Front') {
                    sh 'npm run sonar'
                }
            }
        }
        stage('Package Artifact') {
            steps {
                script {
                    dir('Front') {
                        sh 'tar -czf dist/valoparamadminfront.tgz -C dist .'
                    }
                }
            }
        }
        stage('Deploy to Nexus') {
            steps {
                script {
                    dir('Front') {
                        def packageJson = readJSON file: 'package.json'
                        def packageName = packageJson.name
                        def packageVersion = packageJson.version
                        def username = 'admin'
                        def password = 'nexus'
                        def npmPackagePath = "dist/valoparamadminfront.tgz"

                        sh """
                        curl -u ${username}:${password} --upload-file ${npmPackagePath} http://192.168.33.10:8081/repository/maven-releases/${packageName}-${packageVersion}.tgz
                        """
                    }
                }
            }
        }
        stage('Docker Build Image') {
            steps {
                dir('Front') {
                    sh 'docker build -t fediijat/valoparamadminfront .'
                }
            }
        }
        stage('Docker Push') {
            steps {
                dir('Front') {
                    sh 'docker push fediijat/valoparamadminfront'
                }
            }
        }
    }
}
